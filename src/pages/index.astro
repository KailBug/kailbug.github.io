---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const articles = (
	await getCollection('blog', ({ data }) => !data.draft)
).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const featuredArticles = articles
	.filter(({ data }) => data.featured)
	.slice(0, 3);

const latestArticles = articles.slice(0, 4);

const dateFormatter = new Intl.DateTimeFormat('zh-CN', {
	year: 'numeric',
	month: 'short',
	day: 'numeric',
	timeZone: 'UTC',
});
---

<BaseLayout
	title="KailBug — Agent Engineering"
	description="Writing about Agent Runtime, Harness, Tool Use, Memory, Evaluation and Agent Infrastructure."
>
	<main class="home">
		<section class="hero">
			<div class="hero-copy">
				<p class="eyebrow">AGENT ENGINEERING / SOFTWARE ENGINEERING</p>

				<h1>Building reliable<br />AI agents.</h1>

				<p class="intro">
					Writing about Agent Runtime, Harness, Tool Use,
					Memory, Evaluation and Agent Infrastructure.
				</p>

				<div class="actions">
					<a class="primary-action" href="/articles/">
						Browse Articles
					</a>

					<a
						class="secondary-action"
						href="https://github.com/KailBug"
						target="_blank"
						rel="noreferrer"
					>
						GitHub ↗
					</a>
				</div>
			</div>

			<aside class="focus-card" aria-label="技术方向">
				<p class="focus-label">CURRENT FOCUS</p>

				<ul>
					<li>Agent Runtime & Harness</li>
					<li>Long-Horizon Agent Loop</li>
					<li>Context & Memory Systems</li>
					<li>Evaluation & Observability</li>
					<li>Agent Infrastructure</li>
				</ul>
			</aside>
		</section>

		<section class="content-section">
			<div class="section-heading">
				<div>
					<p class="section-label">SELECTED WRITING</p>
					<h2>Featured Articles</h2>
				</div>

				<a href="/articles/">View all →</a>
			</div>

			<div class="article-grid">
				{
					featuredArticles.map((article) => (
						<article class="article-card">
							<div class="article-meta">
								<span>{article.data.category}</span>
								<time datetime={article.data.date.toISOString()}>
									{dateFormatter.format(article.data.date)}
								</time>
							</div>

							<h3>
								<a href={`/articles/${article.id}/`}>
									{article.data.title}
								</a>
							</h3>

							<p>{article.data.description}</p>

							<ul class="tags" aria-label="标签">
								{article.data.tags.slice(0, 3).map((tag) => <li>{tag}</li>)}
							</ul>
						</article>
					))
				}
			</div>
		</section>

		<section class="content-section">
			<div class="section-heading">
				<div>
					<p class="section-label">RECENTLY PUBLISHED</p>
					<h2>Latest Articles</h2>
				</div>
			</div>

			<div class="latest-list">
				{
					latestArticles.map((article) => (
						<a
							class="latest-item"
							href={`/articles/${article.id}/`}
						>
							<div>
                <span class="latest-category">
                  {article.data.category}
                </span>
								<h3>{article.data.title}</h3>
							</div>

							<time datetime={article.data.date.toISOString()}>
								{dateFormatter.format(article.data.date)}
							</time>
						</a>
					))
				}
			</div>
		</section>

		<section class="projects-section">
			<div>
				<p class="section-label">BUILDING IN PUBLIC</p>
				<h2>Projects & Experiments</h2>
				<p>
					Open-source contributions, Agent experiments
					and personal software projects.
				</p>
			</div>

			<a href="/projects/">Explore Projects →</a>
		</section>
	</main>
</BaseLayout>

<style>
	.home {
		width: min(1100px, calc(100% - 40px));
		margin: 0 auto;
	}

	.hero {
		display: grid;
		grid-template-columns: minmax(0, 1.5fr) minmax(280px, 0.7fr);
		align-items: center;
		gap: 72px;
		min-height: 620px;
		padding: 96px 0;
	}

	.eyebrow,
	.section-label,
	.focus-label {
		color: var(--color-accent);
		font-size: 0.78rem;
		font-weight: 750;
		letter-spacing: 0.14em;
	}

	h1 {
		margin: 20px 0 24px;
		font-size: clamp(3.5rem, 8vw, 7rem);
		line-height: 0.95;
		letter-spacing: -0.065em;
	}

	.intro {
		max-width: 650px;
		margin: 0;
		color: var(--color-muted);
		font-size: clamp(1.1rem, 2vw, 1.35rem);
		line-height: 1.75;
	}

	.actions {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		margin-top: 36px;
	}

	.actions a {
		padding: 12px 18px;
		border-radius: 9px;
		font-weight: 650;
		text-decoration: none;
	}

	.primary-action {
		background: var(--color-text);
		color: white;
	}

	.primary-action:hover {
		background: var(--color-accent);
	}

	.secondary-action {
		border: 1px solid var(--color-border);
		background: var(--color-surface);
	}

	.focus-card {
		padding: 30px;
		border: 1px solid var(--color-border);
		border-radius: 18px;
		background: var(--color-surface);
		box-shadow: 0 18px 50px rgb(32 50 82 / 8%);
	}

	.focus-card ul {
		display: grid;
		gap: 16px;
		margin: 24px 0 0;
		padding: 0;
		list-style: none;
	}

	.focus-card li {
		padding-bottom: 16px;
		border-bottom: 1px solid var(--color-border);
	}

	.focus-card li:last-child {
		padding-bottom: 0;
		border-bottom: 0;
	}

	.content-section {
		padding: 72px 0;
		border-top: 1px solid var(--color-border);
	}

	.section-heading {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 24px;
		margin-bottom: 32px;
	}

	.section-heading h2,
	.projects-section h2 {
		margin: 8px 0 0;
		font-size: clamp(2rem, 4vw, 3rem);
		letter-spacing: -0.04em;
	}

	.section-heading > a {
		color: var(--color-muted);
		text-decoration: none;
	}

	.section-heading > a:hover {
		color: var(--color-accent);
	}

	.article-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 20px;
	}

	.article-card {
		padding: 26px;
		border: 1px solid var(--color-border);
		border-radius: 16px;
		background: var(--color-surface);
	}

	.article-meta {
		display: flex;
		justify-content: space-between;
		gap: 12px;
		color: #68758a;
		font-size: 0.8rem;
	}

	.article-card h3 {
		margin: 18px 0 12px;
		font-size: 1.25rem;
		line-height: 1.4;
	}

	.article-card h3 a {
		text-decoration: none;
	}

	.article-card h3 a:hover {
		color: var(--color-accent);
	}

	.article-card > p {
		color: var(--color-muted);
		line-height: 1.65;
	}

	.tags {
		display: flex;
		flex-wrap: wrap;
		gap: 7px;
		margin: 20px 0 0;
		padding: 0;
		list-style: none;
	}

	.tags li {
		padding: 4px 9px;
		border-radius: 999px;
		background: #eef4ff;
		color: #315a9d;
		font-size: 0.75rem;
	}

	.latest-list {
		border-top: 1px solid var(--color-border);
	}

	.latest-item {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 32px;
		padding: 24px 4px;
		border-bottom: 1px solid var(--color-border);
		text-decoration: none;
	}

	.latest-item:hover h3 {
		color: var(--color-accent);
	}

	.latest-item h3 {
		margin: 6px 0 0;
		font-size: 1.15rem;
	}

	.latest-category,
	.latest-item time {
		color: var(--color-muted);
		font-size: 0.8rem;
	}

	.projects-section {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 48px;
		margin: 72px 0;
		padding: 48px;
		border-radius: 20px;
		background: var(--color-text);
		color: white;
	}

	.projects-section p:not(.section-label) {
		max-width: 620px;
		color: #bdc7d8;
		line-height: 1.7;
	}

	.projects-section a {
		flex: none;
		color: white;
		font-weight: 700;
		text-decoration: none;
	}

	@media (max-width: 820px) {
		.hero {
			grid-template-columns: 1fr;
			min-height: auto;
			gap: 40px;
			padding: 72px 0;
		}

		.article-grid {
			grid-template-columns: 1fr;
		}

		.projects-section {
			align-items: flex-start;
			flex-direction: column;
		}
	}

	@media (max-width: 560px) {
		.section-heading,
		.latest-item {
			align-items: flex-start;
			flex-direction: column;
		}

		.projects-section {
			padding: 32px 24px;
		}
	}
</style>